import { Text, View } from 'react-native';

const ProfileScreen = () => {
  return (
    <View>
      <Text style={{ alignItems: 'center', flex: 1, justifyContent: 'center' }}>
        프로필스크린
      </Text>
    </View>
  );
};

export default ProfileScreen;
